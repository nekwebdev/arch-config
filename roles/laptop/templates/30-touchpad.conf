Section "InputClass"
        Identifier "libinput touchpad catchall"
        MatchIsTouchpad "on"
        MatchDevicePath "/dev/input/event*"
        Driver "libinput"
        Option "NaturalScrolling" "{{ libinput.natural_scroll }}"
        Option "AccelSpeed" "{{ libinput.accel_speed }}"
EndSection
